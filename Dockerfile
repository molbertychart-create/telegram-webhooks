# Используем официальный PHP образ с поддержкой веб-сервера
FROM php:8.2-apache

# Копируем все файлы из текущей папки в контейнер
COPY . /var/www/html/

# Включаем модуль rewrite (может понадобиться для маршрутов)
RUN a2enmod rewrite

# Устанавливаем кодировку по умолчанию
ENV LANG=C.UTF-8

# Открываем порт 80 для веб-сервера
EXPOSE 80

# Контейнер по умолчанию запускает Apache в режиме фореграунд
CMD ["apache2-foreground"]

